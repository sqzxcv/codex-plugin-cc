# Upstream 1.0.6 Merge Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan inline. Do not push changes.

**Goal:** Merge official OpenAI Codex plugin v1.0.6 into the local session-review fork without losing fork identity or behavior.

**Architecture:** Merge `openai/main` into local `main`, retain the fork's `sqzxcv` publishing metadata and `1.1.0` release version, and accept the upstream process/Git hardening plus regression test. Review every upstream file against session-review call paths, then run focused and full verification.

**Tech Stack:** Git, Node.js ESM, Node test runner, Claude Code plugin manifests.

## Global Constraints

- Preserve marketplace name `sq-codex`, package scope `@sqzxcv`, and plugin version `1.1.0`.
- Preserve all session-review commands, runtime modules, tests, and documentation.
- Merge official tag `v1.0.6` from remote `openai`.
- Do not push changes.

---

### Task 1: Merge official v1.0.6

**Files:**
- Merge: `.claude-plugin/marketplace.json`
- Merge: `package.json`
- Merge: `package-lock.json`
- Merge: `plugins/codex/.claude-plugin/plugin.json`
- Accept upstream: `plugins/codex/scripts/lib/git.mjs`
- Accept upstream: `plugins/codex/scripts/lib/process.mjs`
- Accept upstream: `tests/git.test.mjs`
- Accept upstream: `tests/helpers.mjs`

**Interfaces:**
- Consumes: `openai/main` at official tag `v1.0.6`.
- Produces: local `main` containing upstream commit `db52e28` and fork metadata at `1.1.0`.

- [x] Merge `openai/main` without pushing.
- [x] Resolve the four metadata conflicts by retaining the local fork versions.
- [x] Confirm no session-review file is deleted or replaced.

### Task 2: Verify security behavior and session-review compatibility

**Files:**
- Review: `plugins/codex/scripts/lib/git.mjs`
- Review: `plugins/codex/scripts/lib/process.mjs`
- Modify: `plugins/codex/scripts/lib/session-review.mjs`
- Test: `tests/git.test.mjs`
- Modify: `tests/session-review.test.mjs`

**Interfaces:**
- Consumes: `runCommand(command, args, { shell })` and Git context collection.
- Produces: literal Git argument handling on Windows while leaving Codex process invocation behavior unchanged.

- [x] Run the upstream Git regression test and session-review tests.
- [x] Trace session-review Git collection, add `shell: false`, and document the effect.
- [x] Run the complete test suite, build, version check, plugin validation, and `git diff --check`.
- [x] Review the final merge diff and repository status before delivery.
