description = "Show the stored local Codex result for the current workspace and return the output verbatim."
prompt = """
Use the MCP tool `codex_result` exactly once.

Pass `raw_args` as the exact argument text from this slash command: `{{args}}`.

After the tool returns, reply with the tool output verbatim and nothing else.
Do not use `run_shell_command`.
"""
